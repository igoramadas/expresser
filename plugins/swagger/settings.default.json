// EXPRESSER SWAGGER SETTINGS
{
    "swagger": {
        // Enable the Swagger module?
        "enabled": true,
        // Filename of swagger template file to load specs from.
        "filename": "swagger.template.json",
        // You can also define the template specs directly on the specs key below.
        "specs": null,
        // Auto match the API version with the app's package version?
        "usePackageVersion": true
    }
}
