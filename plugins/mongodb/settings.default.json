// EXPRESSER MONGODB SETTINGS
{
    "mongodb": {
        // Enable MongoDB module by default.
        "enabled": true,
        // If you want to use only one database, you can set the default connection string
        // which will be instantiated automatically on init.
        "connString": null,
        // Database connection options.
        "options": {
            // Default pool size for connections is 10.
            "poolSize": 10
        }
    }
}
