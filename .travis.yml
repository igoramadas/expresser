sudo: false
language: node_js
node_js:
  - "9.4"
  - "9.3"
  - "8.9"
services:
  - mongodb
before_script:
  - npm install loggly
  - npm install mongodb
  - npm install node-logentries
  - npm install nodemailer
  - npm install socket.io
after_script:
  - NODE_ENV=test istanbul cover ./node_modules/mocha/bin/_mocha --exit --report lcovonly -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
